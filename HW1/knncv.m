function cv_error = knncv(traindata, trainlabels, n, k, f, D)
% traindata = Matrix(ntrain x ndim)
%           --> Each row is one picture, each col is one feature.
% trainlabels = Vector (ntrain x 1)
% n = partition train into n parts
% k = number of neighbors
% D = Matrix (ntrain x ntest)

cv_error = 99;

% Plot train error: 
% i.e. if you train on all 800 images, and make predictions on these same 800
%images
end

